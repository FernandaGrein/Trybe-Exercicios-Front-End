
const INITIAL_STATE = {
    imagePath: '',
    isFetch: false, 
    error:'',
}


const dogReducer = ( state = INITIAL_STATE, action ) => {
    switch(action.type) {
        default: 
        return state;
    }
};

export default dogReducer;
